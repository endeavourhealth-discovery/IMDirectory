# Application Login

This specification describes the login functionality of IMDirectory.

* Open IMDirectory

## Login Snapshot
* Navigate to the login page
  * routes to casdoor
* Type "cypress" into the "username" field
* Type the password into the "password" field
* Click "Sign In"
  * routes to IMDirectory
  * user is logged in
