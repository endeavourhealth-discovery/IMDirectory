{
  "name": "im",
  "version": "0.0.0",
  "description": "IM app suite",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/node": "^18.11.11",
    "lerna": "6.1.0",
    "typescript": "^4.9.3"
  },
  "scripts": {
    "lerna:tsc": "lerna run tsc",
    "lerna:build": "lerna run tsc && lerna run build"
  }
}
